generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       String  @id @default(uuid())
  name     String
  email    String  @unique
  password String
  cpf      String
  isAdmin  Boolean

  profile Profile[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
}

model Profile {
  id       String @id @default(uuid())
  title    String @unique
  imageURL String


  user   User   @relation(fields: [userId], references: [id])
  userId String @map("user_id")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
}

model Game {
  id                 String @id @default(uuid())
  title              String @unique
  coverImageUrl      String
  description        String
  year               Int
  imdbScore          Int
  trailerYouTubeUrl  String
  gameplayYouTubeUrl String


  genre Genre[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
}

model Genre {
  id   String @id @default(uuid())
  name String @unique


  game Game[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
}

model Homepage {
  id String @id @default(uuid())

  // Jogos favoritos
  //Sessao genero Jogos classificados por genero

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("homepage")
}
